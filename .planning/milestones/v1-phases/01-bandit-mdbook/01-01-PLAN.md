---
phase: 01-bandit-mdbook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Bandit/Bandit.sln
  - Bandit/src/Bandit/Bandit.fsproj
  - Bandit/src/Bandit/Domain.fs
  - Bandit/src/Bandit.Console/Bandit.Console.fsproj
  - Bandit/src/Bandit.Console/Program.fs
  - Bandit/tests/Bandit.Tests/Bandit.Tests.fsproj
  - Bandit/tests/Bandit.Tests/Main.fs
  - tutorial/book.toml
  - tutorial/src/SUMMARY.md
  - tutorial/src/README.md
  - tutorial/src/01-bandit/README.md
autonomous: true

must_haves:
  truths:
    - "`dotnet build Bandit/Bandit.sln` succeeds with zero errors"
    - "`mdbook build tutorial/` succeeds and emits `tutorial/book/` output"
    - "Three-project layout exists: Bandit (pure library), Bandit.Console (impure shell), Bandit.Tests (test runner)"
    - "mdBook SUMMARY.md references 01-bandit/README.md and the file exists"
  artifacts:
    - path: "Bandit/Bandit.sln"
      provides: "Solution file referencing all three projects"
      contains: "Bandit.fsproj"
    - path: "Bandit/src/Bandit/Bandit.fsproj"
      provides: "Pure library project (net9.0, classlib)"
      contains: "Domain.fs"
    - path: "Bandit/src/Bandit/Domain.fs"
      provides: "Core types: Arm, AgentState, BanditEnv (stub)"
      min_lines: 10
    - path: "Bandit/src/Bandit.Console/Bandit.Console.fsproj"
      provides: "Console entrypoint (net9.0, Exe) referencing Bandit.fsproj"
      contains: "Serilog"
    - path: "Bandit/tests/Bandit.Tests/Bandit.Tests.fsproj"
      provides: "Test runner (net9.0, Exe) referencing Bandit.fsproj"
      contains: "Expecto"
    - path: "tutorial/book.toml"
      provides: "mdBook config with Korean title"
      contains: "F#으로 배우는 강화학습"
    - path: "tutorial/src/SUMMARY.md"
      provides: "Chapter TOC linking all 5 phase folders"
      contains: "01-bandit"
  key_links:
    - from: "Bandit/src/Bandit.Console/Bandit.Console.fsproj"
      to: "Bandit/src/Bandit/Bandit.fsproj"
      via: "ProjectReference"
      pattern: "ProjectReference.*Bandit\\.fsproj"
    - from: "Bandit/tests/Bandit.Tests/Bandit.Tests.fsproj"
      to: "Bandit/src/Bandit/Bandit.fsproj"
      via: "ProjectReference"
      pattern: "ProjectReference.*Bandit\\.fsproj"
    - from: "tutorial/src/SUMMARY.md"
      to: "tutorial/src/01-bandit/README.md"
      via: "Markdown link"
      pattern: "01-bandit/README\\.md"
---

<objective>
Bootstrap the complete project skeleton: the Bandit F# solution (three projects), all NuGet packages, and the mdBook tutorial site with Korean content stubs for all five phases.

Purpose: Every subsequent plan in this phase (and every future phase) depends on this skeleton existing and building cleanly. Nothing can proceed until `dotnet build` and `mdbook build` both pass.

Output:
- `Bandit/` — three-project F# solution with pinned NuGet packages and placeholder source files
- `tutorial/` — mdBook with Korean book.toml, SUMMARY.md covering all 5 chapters, and a stub 01-bandit/README.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bandit-mdbook/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Bandit F# solution with three projects and NuGet packages</name>
  <files>
    Bandit/Bandit.sln
    Bandit/src/Bandit/Bandit.fsproj
    Bandit/src/Bandit/Domain.fs
    Bandit/src/Bandit/Environment.fs
    Bandit/src/Bandit/Agent.fs
    Bandit/src/Bandit/Training.fs
    Bandit/src/Bandit.Console/Bandit.Console.fsproj
    Bandit/src/Bandit.Console/Program.fs
    Bandit/tests/Bandit.Tests/Bandit.Tests.fsproj
    Bandit/tests/Bandit.Tests/PropertyTests.fs
    Bandit/tests/Bandit.Tests/ConvergenceTests.fs
    Bandit/tests/Bandit.Tests/Main.fs
  </files>
  <action>
Run these commands from the repo root to create the solution and projects:

```bash
mkdir -p Bandit
cd Bandit
dotnet new sln -n Bandit

dotnet new classlib -lang F# -o src/Bandit --framework net9.0
dotnet new console -lang F# -o src/Bandit.Console --framework net9.0
dotnet new console -lang F# -o tests/Bandit.Tests --framework net9.0

dotnet sln add src/Bandit/Bandit.fsproj
dotnet sln add src/Bandit.Console/Bandit.Console.fsproj
dotnet sln add tests/Bandit.Tests/Bandit.Tests.fsproj

dotnet add src/Bandit.Console/Bandit.Console.fsproj reference src/Bandit/Bandit.fsproj
dotnet add tests/Bandit.Tests/Bandit.Tests.fsproj reference src/Bandit/Bandit.fsproj

dotnet add src/Bandit.Console/Bandit.Console.fsproj package Serilog --version 4.3.1
dotnet add src/Bandit.Console/Bandit.Console.fsproj package Serilog.Sinks.Console --version 6.1.1
dotnet add src/Bandit.Console/Bandit.Console.fsproj package Serilog.Sinks.File --version 7.0.0

dotnet add tests/Bandit.Tests/Bandit.Tests.fsproj package Expecto --version 10.2.3
dotnet add tests/Bandit.Tests/Bandit.Tests.fsproj package Expecto.FsCheck --version 10.2.3
dotnet add tests/Bandit.Tests/Bandit.Tests.fsproj package FsCheck --version 3.3.2
```

Then configure file ordering in `Bandit/src/Bandit/Bandit.fsproj`. Remove the auto-generated `Library.fs` and set up the correct file order in the `<ItemGroup>` (CRITICAL — F# compiler reads files top-to-bottom):

```xml
<ItemGroup>
  <Compile Include="Domain.fs" />
  <Compile Include="Environment.fs" />
  <Compile Include="Agent.fs" />
  <Compile Include="Training.fs" />
</ItemGroup>
```

Write stub source files for the Bandit library (these will be filled in Plan 02):

**Bandit/src/Bandit/Domain.fs** — core types only, no logic:
```fsharp
module Bandit.Domain

/// Index of an arm (0-based)
type Arm = int

/// Immutable agent state: visit counts and estimated values per arm
type AgentState = {
    Counts: int array
    Values: float array
}

/// Bandit environment: N arms with fixed reward probabilities
type BanditEnv = {
    RewardProbs: float array
}
```

**Bandit/src/Bandit/Environment.fs** — stub:
```fsharp
module Bandit.Environment

open Bandit.Domain

/// Pull an arm and return reward (1.0 or 0.0) — pure, rng passed as parameter
let pullArm (rng: System.Random) (env: BanditEnv) (arm: Arm) : float =
    if rng.NextDouble() < env.RewardProbs.[arm] then 1.0 else 0.0
```

**Bandit/src/Bandit/Agent.fs** — stub:
```fsharp
module Bandit.Agent

open Bandit.Domain

/// Update estimate using incremental mean: NewVal = OldVal + (1/n) * (reward - OldVal)
let incrementalMean (state: AgentState) (arm: Arm) (reward: float) : AgentState =
    let n = state.Counts.[arm] + 1
    let newVal = state.Values.[arm] + (1.0 / float n) * (reward - state.Values.[arm])
    { Counts = state.Counts |> Array.mapi (fun i c -> if i = arm then n else c)
      Values = state.Values |> Array.mapi (fun i v -> if i = arm then newVal else v) }

/// ε-greedy arm selection — stub (full impl in Plan 02)
let epsilonGreedy (rng: System.Random) (epsilon: float) (state: AgentState) : Arm =
    if rng.NextDouble() < epsilon then rng.Next(state.Values.Length)
    else state.Values |> Array.indexed |> Array.maxBy snd |> fst

/// UCB1 arm selection — stub (full impl in Plan 02)
let ucb1 (totalSteps: int) (state: AgentState) : Arm =
    match Array.tryFindIndex (fun c -> c = 0) state.Counts with
    | Some arm -> arm
    | None ->
        let t = float totalSteps
        state.Values
        |> Array.mapi (fun i q -> q + sqrt (2.0 * log t / float state.Counts.[i]))
        |> Array.indexed
        |> Array.maxBy snd
        |> fst
```

**Bandit/src/Bandit/Training.fs** — stub:
```fsharp
module Bandit.Training

open Bandit.Domain
open Bandit.Environment
open Bandit.Agent

/// Run one episode of N steps, returns final AgentState — pure
let runEpisode
    (rng: System.Random)
    (env: BanditEnv)
    (selectArm: System.Random -> AgentState -> int)
    (steps: int) : AgentState =

    let initial = { Counts = Array.zeroCreate env.RewardProbs.Length
                    Values = Array.zeroCreate env.RewardProbs.Length }
    (initial, List.init steps id)
    ||> List.fold (fun state _ ->
        let arm = selectArm rng state
        let reward = pullArm rng env arm
        incrementalMean state arm reward)
```

Configure `Bandit/tests/Bandit.Tests/Bandit.Tests.fsproj` file ordering:
```xml
<ItemGroup>
  <Compile Include="PropertyTests.fs" />
  <Compile Include="ConvergenceTests.fs" />
  <Compile Include="Main.fs" />
</ItemGroup>
```

Write stub test files:

**Bandit/tests/Bandit.Tests/PropertyTests.fs**:
```fsharp
module Bandit.Tests.PropertyTests

open Expecto
open Bandit.Domain

let propertyTests =
    testList "Property Tests" [
        // Stubs — full tests in Plan 03
        testCase "placeholder" <| fun () -> ()
    ]
```

**Bandit/tests/Bandit.Tests/ConvergenceTests.fs**:
```fsharp
module Bandit.Tests.ConvergenceTests

open Expecto

let convergenceTests =
    testList "Convergence Tests" [
        // Stubs — full tests in Plan 03
        testCase "placeholder" <| fun () -> ()
    ]
```

**Bandit/tests/Bandit.Tests/Main.fs**:
```fsharp
module Bandit.Tests.Main

open Expecto
open Bandit.Tests.PropertyTests
open Bandit.Tests.ConvergenceTests

[<EntryPoint>]
let main args =
    let allTests = testList "All Tests" [ propertyTests; convergenceTests ]
    runTestsWithCLIArgs [] args allTests
```

**Bandit/src/Bandit.Console/Program.fs** — minimal stub that compiles:
```fsharp
module Bandit.Console.Program

open Serilog

[<EntryPoint>]
let main _args =
    Log.Logger <-
        LoggerConfiguration()
            .MinimumLevel.Information()
            .WriteTo.Console(outputTemplate = "[{Timestamp:HH:mm:ss} {Level:u3}] {Message:lj}{NewLine}")
            .CreateLogger()
    Log.Information("Bandit — Phase 1 placeholder")
    Log.CloseAndFlush()
    0
```
  </action>
  <verify>
```bash
cd /Users/ohama/vibe-coding/reinforcement-tutorial/Bandit
dotnet build Bandit.sln
dotnet test tests/Bandit.Tests/Bandit.Tests.fsproj
dotnet run --project src/Bandit.Console/Bandit.Console.fsproj
```
All three commands must succeed with zero errors. `dotnet build` must show "Build succeeded". `dotnet test` must show all tests passed. `dotnet run` must print the Serilog log line.
  </verify>
  <done>
`dotnet build Bandit.sln` exits 0. `dotnet test` exits 0. `dotnet run` prints "[HH:mm:ss INF] Bandit — Phase 1 placeholder". No compiler errors, no warnings about missing files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize mdBook tutorial site with Korean content and all 5 chapter stubs</name>
  <files>
    tutorial/book.toml
    tutorial/src/SUMMARY.md
    tutorial/src/README.md
    tutorial/src/01-bandit/README.md
    tutorial/src/02-tictactoe/README.md
    tutorial/src/03-connect-four/README.md
    tutorial/src/04-dqn/README.md
    tutorial/src/05-gomoku/README.md
  </files>
  <action>
Create the tutorial directory structure at the repo root (NOT inside Bandit/ — mdBook covers all 5 phases):

```bash
mkdir -p tutorial/src/01-bandit
mkdir -p tutorial/src/02-tictactoe
mkdir -p tutorial/src/03-connect-four
mkdir -p tutorial/src/04-dqn
mkdir -p tutorial/src/05-gomoku
```

Write `tutorial/book.toml`:
```toml
[book]
title = "F#으로 배우는 강화학습"
authors = []
description = "슬롯머신부터 오목 AI까지 — F#으로 구현하는 강화학습 단계별 튜토리얼"
language = "ko"
src = "src"

[output.html]
```

Write `tutorial/src/SUMMARY.md`:
```markdown
# 목차

[소개](README.md)

---

# 기초

- [Chapter 1: 슬롯머신 — 탐색과 활용](01-bandit/README.md)
- [Chapter 2: 틱택토 — 상태와 가치](02-tictactoe/README.md)

# 심화

- [Chapter 3: 커넥트 포 — Q-Learning과 Minimax](03-connect-four/README.md)
- [Chapter 4: 커넥트 포 DQN — 신경망으로 일반화](04-dqn/README.md)
- [Chapter 5: 오목 MCTS — 트리 탐색과 자가 대국](05-gomoku/README.md)
```

Write `tutorial/src/README.md`:
```markdown
# F#으로 배우는 강화학습

이 튜토리얼은 F#으로 강화학습(Reinforcement Learning) 핵심 알고리즘을 단계별로 구현하며 배우는 실전 가이드입니다.

## 구성

| Chapter | 게임 | 핵심 개념 |
|---------|------|----------|
| 1 | 슬롯머신 (Bandit) | Exploration vs Exploitation, ε-greedy, UCB1 |
| 2 | 틱택토 | MDP, Value Function, TD Learning |
| 3 | 커넥트 포 | Q-Learning, Minimax, Alpha-Beta Pruning |
| 4 | 커넥트 포 DQN | 신경망, Experience Replay, Target Network |
| 5 | 오목 | MCTS, Policy/Value Network, Self-Play |

## 전제 조건

- F# 기본 문법 숙지
- .NET 9 SDK 설치
- RL 개념 사전 지식 불필요 — 이 튜토리얼에서 다룸
```

Write `tutorial/src/01-bandit/README.md` (stub — full content in Plan 03):
```markdown
# Chapter 1: 슬롯머신 — 탐색과 활용

## 핵심 개념

**Multi-Armed Bandit 문제**: N개의 슬롯머신(arm) 중 어느 것을 당길지 선택하는 문제. 각 arm은 고유한 보상 확률을 가지며, 에이전트는 보상을 최대화해야 한다.

**탐색 vs 활용 (Exploration vs Exploitation)**:
- **탐색**: 새로운 arm을 시도해 더 나은 선택지를 발견
- **활용**: 현재 알고 있는 최선의 arm을 선택

이 두 목표는 서로 충돌한다 — 탐색에 쓴 시간은 활용 기회를 잃는 것이고, 활용만 하면 더 좋은 arm을 놓친다.

## 핵심 F# 타입

```fsharp
/// 슬롯머신의 arm 인덱스 (0-based)
type Arm = int

/// 에이전트의 불변 상태
type AgentState = {
    Counts: int array   // 각 arm을 당긴 횟수
    Values: float array // 각 arm의 추정 가치 (incremental mean)
}

/// Bandit 환경: N개의 arm, 각각 고정된 보상 확률
type BanditEnv = {
    RewardProbs: float array
}
```

## 알고리즘

(상세 내용은 Plan 03에서 작성)

### ε-greedy

확률 ε로 무작위 탐색, 확률 (1-ε)로 현재 최선의 arm 선택.

### UCB1

불확실성이 높은 arm에 탐색 보너스를 부여해 결정론적으로 탐색과 활용을 균형 잡음.
```

Write stub READMEs for the remaining chapters:

**tutorial/src/02-tictactoe/README.md**:
```markdown
# Chapter 2: 틱택토 — 상태와 가치

(Phase 2에서 작성 예정)

MDP, Value Function, TD Learning을 3×3 틱택토로 구현한다.
```

**tutorial/src/03-connect-four/README.md**:
```markdown
# Chapter 3: 커넥트 포 — Q-Learning과 Minimax

(Phase 3에서 작성 예정)

Q-Learning과 Minimax Alpha-Beta Pruning을 비교한다.
```

**tutorial/src/04-dqn/README.md**:
```markdown
# Chapter 4: 커넥트 포 DQN — 신경망으로 일반화

(Phase 4에서 작성 예정)

TorchSharp Conv2D 신경망으로 Q-table 한계를 극복한다.
```

**tutorial/src/05-gomoku/README.md**:
```markdown
# Chapter 5: 오목 MCTS — 트리 탐색과 자가 대국

(Phase 5에서 작성 예정)

MCTS + Policy/Value Network + 자가 대국 파이프라인을 구현한다.
```

Then run mdbook build to verify (mdbook must be installed):
```bash
mdbook build tutorial/
```
  </action>
  <verify>
```bash
mdbook build /Users/ohama/vibe-coding/reinforcement-tutorial/tutorial/
```
Must succeed with exit code 0 and produce `tutorial/book/index.html`. Check that:
1. `tutorial/book/` directory was created
2. `tutorial/src/01-bandit/README.md` exists
3. No "file not found" warnings in mdbook output
  </verify>
  <done>
`mdbook build tutorial/` exits 0. `tutorial/book/index.html` exists. All five chapter README.md files exist under `tutorial/src/`. SUMMARY.md links resolve without errors.

If mdbook is not installed: run `cargo install mdbook --version 0.4.52` or download the binary from https://github.com/rust-lang/mdBook/releases/tag/v0.4.52. If cargo/mdbook unavailable, verify by checking that all files exist and SUMMARY.md links are consistent — build verification deferred to Plan 03.
  </done>
</task>

</tasks>

<verification>
Run from repo root:
```bash
cd /Users/ohama/vibe-coding/reinforcement-tutorial/Bandit && dotnet build Bandit.sln && dotnet test && dotnet run --project src/Bandit.Console/Bandit.Console.fsproj
cd /Users/ohama/vibe-coding/reinforcement-tutorial && mdbook build tutorial/
```
All commands exit 0. Three F# projects build. Test runner finds and passes placeholder tests. mdBook emits `tutorial/book/`.
</verification>

<success_criteria>
- `Bandit/Bandit.sln` exists and references three projects
- `dotnet build Bandit/Bandit.sln` exits 0 (no errors)
- `dotnet test Bandit/tests/Bandit.Tests/` exits 0
- `dotnet run --project Bandit/src/Bandit.Console/` prints Serilog log line and exits 0
- `tutorial/book.toml` contains Korean title
- `tutorial/src/SUMMARY.md` links all 5 chapter READMEs
- All 5 chapter stub READMEs exist
- `mdbook build tutorial/` exits 0
- F# file order in Bandit.fsproj: Domain.fs → Environment.fs → Agent.fs → Training.fs
- No Serilog in Bandit.Tests (test project has no logging dependency)
</success_criteria>

<output>
After completion, create `.planning/phases/01-bandit-mdbook/01-01-SUMMARY.md` following the summary template.
</output>
