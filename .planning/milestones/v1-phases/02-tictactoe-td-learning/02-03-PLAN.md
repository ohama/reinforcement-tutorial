---
phase: 02-tictactoe-td-learning
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - TicTacToe/src/TicTacToe.Console/Program.fs
  - tutorial/src/02-tictactoe/README.md
autonomous: true

must_haves:
  truths:
    - "dotnet run --project TicTacToe/src/TicTacToe.Console 실행 시 학습 시작, Serilog 로그 출력, 사람 vs AI 대전 콘솔이 시작된다"
    - "Serilog가 매 1000판마다 Episode=N WinRate=X% 형식의 구조화 로그를 출력한다"
    - "사람이 1-9 숫자를 입력하면 AI와 번갈아 둘 수 있고 게임 결과가 출력된다"
    - "mdbook build tutorial/ 성공하고 02-tictactoe 챕터에 {{#include}}로 실제 소스가 인클루드된다"
    - "mdBook 챕터에 Phase 1 Bandit의 한계와 MDP 필요성이 한국어로 설명된다"
  artifacts:
    - path: "TicTacToe/src/TicTacToe.Console/Program.fs"
      provides: "Serilog 설정, trainAgent 호출, win-rate 로깅, runHumanVsAI 콘솔 루프"
      min_lines: 60
    - path: "tutorial/src/02-tictactoe/README.md"
      provides: "Phase 2 한국어 mdBook 챕터"
      contains: "{{#include"
  key_links:
    - from: "TicTacToe/src/TicTacToe.Console/Program.fs"
      to: "TicTacToe/src/TicTacToe/Training.fs"
      via: "open TicTacToe.Training"
      pattern: "open TicTacToe\\.Training"
    - from: "tutorial/src/02-tictactoe/README.md"
      to: "TicTacToe/src/TicTacToe/Domain.fs"
      via: "{{#include ../../../TicTacToe/src/TicTacToe/Domain.fs}}"
      pattern: "\\{\\{#include.*Domain\\.fs\\}\\}"
---

<objective>
사람 vs AI 콘솔 모드 + Serilog 학습 곡선 로깅 + 한국어 mdBook 02-tictactoe 챕터 완성

Purpose: 학습된 TD 에이전트와 사람이 콘솔에서 직접 대전할 수 있는 인터페이스를 구현하고, Serilog로 학습 곡선을 기록하며, mdBook에 Phase 2 한국어 튜토리얼 챕터를 완성한다.
Output: Program.fs (Serilog + 학습 루프 + 사람 vs AI), tutorial/src/02-tictactoe/README.md — Phase 2 완전히 동작하는 상태
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-tictactoe-td-learning/02-RESEARCH.md
@.planning/phases/02-tictactoe-td-learning/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Program.fs 구현 — Serilog 로깅 + 학습 루프 + 사람 vs AI 콘솔</name>
  <files>
    TicTacToe/src/TicTacToe.Console/Program.fs
  </files>
  <action>
`TicTacToe/src/TicTacToe.Console/Program.fs`를 02-01에서 작성한 스텁에서 완전한 구현으로 교체한다.

CRITICAL 설계 원칙:
- Program.fs는 유일한 불순 파일 (I/O, Serilog, System.Random 초기화)
- Training.fs의 trainAgent는 순수: win-rate history를 반환하고 로그하지 않음
- 로깅은 Program.fs에서만: history를 순회하며 Serilog로 출력
- runHumanVsAI: 재귀 루프, 잘못된 입력은 재시도 (종료 없음)

```fsharp
module TicTacToe.Console.Program

open Serilog
open TicTacToe.Domain
open TicTacToe.Rules
open TicTacToe.Agent
open TicTacToe.Training

/// 보드를 콘솔에 출력
let printBoard (board: Board) =
    let cellChar = function X -> "X" | O -> "O" | Empty -> "."
    for row in 0..2 do
        let line =
            [0..2]
            |> List.map (fun col -> cellChar board.[row * 3 + col])
            |> String.concat " | "
        printfn " %s " line
        if row < 2 then printfn " ---------"

/// 사람(X) vs AI(O) 콘솔 대전 루프 (TICT-06)
/// epsilon=0: AI는 순수 탐욕적 플레이 (최선 수 선택)
let runHumanVsAI (rng: System.Random) (vTable: ValueTable) =
    printfn "\n============================="
    printfn " 사람(X) vs AI(O) 대전 시작! "
    printfn "============================="
    printfn "위치 안내: 1|2|3 / 4|5|6 / 7|8|9 (1=왼쪽 위, 9=오른쪽 아래)\n"

    let rec loop state =
        printBoard state.Board
        match isGameOver state.Board with
        | Some XWins -> printfn "\n사람(X) 승리!"
        | Some OWins -> printfn "\nAI(O) 승리!"
        | Some Draw  -> printfn "\n무승부!"
        | None ->
            if state.CurrentPlayer = X then
                // 사람 차례
                printf "위치 입력 (1-9): "
                let input = System.Console.ReadLine()
                match System.Int32.TryParse(input) with
                | true, n when n >= 1 && n <= 9 ->
                    let idx = n - 1  // 0-indexed로 변환
                    if state.Board.[idx] <> Empty then
                        printfn "이미 둔 자리입니다. 다시 선택하세요."
                        loop state
                    else
                        let nextBoard = applyMove state.Board state.CurrentPlayer idx
                        let nextState = { Board = nextBoard; CurrentPlayer = otherPlayer state.CurrentPlayer }
                        loop nextState
                | _ ->
                    printfn "잘못된 입력입니다. 1-9 사이 숫자를 입력하세요."
                    loop state
            else
                // AI 차례 (epsilon=0: 순수 탐욕)
                let move = tdAgent rng 0.0 vTable state
                printfn "AI(O)가 %d번 위치에 두었습니다." (move + 1)
                let nextBoard = applyMove state.Board state.CurrentPlayer move
                let nextState = { Board = nextBoard; CurrentPlayer = otherPlayer state.CurrentPlayer }
                loop nextState

    loop (initialState ())

[<EntryPoint>]
let main _args =
    // Serilog 설정 (TICT-09)
    Log.Logger <-
        LoggerConfiguration()
            .MinimumLevel.Information()
            .WriteTo.Console(
                outputTemplate = "[{Timestamp:HH:mm:ss} {Level:u3}] {Message:lj}{NewLine}")
            .WriteTo.File(
                "logs/tictactoe-.log",
                rollingInterval = RollingInterval.Day,
                outputTemplate = "[{Timestamp:HH:mm:ss} {Level:u3}] {Message:lj}{NewLine}")
            .CreateLogger()

    let rng = System.Random(42)

    Log.Information("=== TicTacToe TD Learning 시작 ===")
    Log.Information("학습 설정: 에피소드=100,000 alpha=0.1 epsilon=0.1 로그간격=1,000")

    // 학습 실행 (TICT-05): trainAgent는 순수 함수로 history 반환
    let vTable, history = trainAgent rng 100_000 0.1 0.1 1_000

    // 학습 곡선 로깅 (TICT-09): 매 1,000 에피소드 승률 기록
    for (ep, rate) in history do
        Log.Information("Episode={Episode} WinRate={WinRate:P1}", ep, rate)

    let finalRate = if history.IsEmpty then 0.0 else snd (List.last history)
    Log.Information("학습 완료. 최종 승률: {WinRate:P1}", finalRate)
    Log.Information("ValueTable 크기: {Size}개 상태", Map.count vTable)

    // 사람 vs AI 대전 (TICT-06)
    runHumanVsAI rng vTable

    Log.CloseAndFlush()
    0
```

**빌드 및 실행 확인:**
```bash
# TicTacToe/ 디렉터리에서
dotnet build TicTacToe.sln
# 빌드 성공 후 실행 테스트 (Ctrl+C로 종료):
# dotnet run --project src/TicTacToe.Console
```
  </action>
  <verify>
```bash
cd /Users/ohama/vibe-coding/reinforcement-tutorial/TicTacToe
dotnet build TicTacToe.sln
```
"Build succeeded" 확인. Program.fs 관련 오류 없음.
  </verify>
  <done>
- Program.fs: Serilog 설정 (Console + File sink), trainAgent 호출, history 순회 로깅, runHumanVsAI
- 사람 입력 처리: 1-9 범위 검증, 이미 둔 자리 재시도, 잘못된 입력 재시도
- AI는 epsilon=0으로 순수 탐욕 플레이
- Serilog 구조화 로그: Episode={N} WinRate={X}% 형식
- `dotnet build TicTacToe.sln` 성공
  </done>
</task>

<task type="auto">
  <name>Task 2: 한국어 mdBook 02-tictactoe 챕터 완성</name>
  <files>
    tutorial/src/02-tictactoe/README.md
  </files>
  <action>
`tutorial/src/02-tictactoe/README.md` 파일을 완전한 한국어 챕터로 교체한다.

CRITICAL: {{#include}} 경로는 tutorial/src/ 디렉터리 기준 상대경로다.
- tutorial/src/02-tictactoe/README.md → TicTacToe/src/TicTacToe/Domain.fs
- 경로: ../../../TicTacToe/src/TicTacToe/Domain.fs
- (tutorial/src/02-tictactoe/에서 3단계 위로 이동하면 repo root = tutorial/../ = repo root가 아님!)

실제 경로 계산:
- README.md 위치: tutorial/src/02-tictactoe/README.md
- Domain.fs 위치: TicTacToe/src/TicTacToe/Domain.fs
- 상대경로: ../../../TicTacToe/src/TicTacToe/Domain.fs
  (02-tictactoe/ → src/ → tutorial/ → repo root → TicTacToe/...)

이 경로를 실제로 존재하는 파일로 먼저 확인:
```bash
ls tutorial/src/../../TicTacToe/src/TicTacToe/Domain.fs 2>/dev/null || echo "경로 확인 필요"
# 또는
ls TicTacToe/src/TicTacToe/Domain.fs
```

파일 내용 (TUTR-03, TUTR-04, TUTR-05, TUTR-06):

```markdown
# Chapter 2: 틱택토 — 상태, 가치, 그리고 시간차 학습

## Phase 1의 한계: 왜 MDP가 필요한가? (TUTR-05)

1장에서 우리는 슬롯머신 문제(Multi-Armed Bandit)를 풀었다. Bandit에서는 **상태가 하나**였다.
어떤 버튼을 눌러도 세상은 그대로였고, 과거의 선택이 미래 선택지에 영향을 주지 않았다.

틱택토는 본질적으로 다르다. **행동이 상태를 바꾼다.** 한 수를 두면 보드가 달라지고,
다음에 선택할 수 있는 자리도 달라진다. 게임이 끝날 때까지 이 과정이 반복된다.

이것이 **MDP(Markov Decision Process)**의 핵심이다:

| 개념 | Bandit | 틱택토 |
|------|--------|--------|
| 상태 | 단 하나 (항상 같은 상황) | 수천 개 (보드 배치) |
| 행동의 영향 | 없음 | 상태 전이 |
| 보상 시점 | 즉각적 | 게임 종료 시 |
| 최적 전략 | 최고 기댓값 arm 선택 | 미래 상태까지 고려 |

Bandit에서는 `incrementalMean`으로 평균 보상만 추적하면 됐다.
틱택토에서는 **"이 보드 배치에서 이길 확률이 얼마인가"**를 추적해야 한다 — 이것이 **가치 함수**다.

## MDP 핵심 개념

**상태 (State):** 현재 보드 배치 (9칸 × {빈칸, X, O} = 최대 19,683가지, 실제 도달 가능 ~5,478가지)

**행동 (Action):** 빈 칸에 돌 놓기 (최대 9개, 게임 진행에 따라 감소)

**전이 (Transition):** `applyMove board player move` → 새 보드

**보상 (Reward):** 게임 종료 시에만 발생 (승: 1.0, 패: 0.0, 무: 0.5)

**가치 함수 (Value Function):** `V(s)` = 상태 s에서 X가 이길 확률

## TD(0) 학습: 시간차 업데이트

**TD Learning (Temporal Difference Learning)**은 실제 게임 플레이를 통해 가치 함수를 업데이트한다.
몬테카를로 방법처럼 게임이 끝날 때까지 기다리지 않고, **매 수마다 즉각 업데이트**한다.

업데이트 공식:

```
V(s) ← V(s) + α × (V(s') - V(s))
```

- `V(s)`: 현재 상태의 가치 추정치
- `V(s')`: 다음 상태의 가치 추정치
- `α (alpha)`: 학습률 (0.1 ~ 0.3)
- `V(s') - V(s)`: TD 오차 (예측이 얼마나 틀렸나)

직관: **다음 상태가 현재보다 좋아 보이면 현재 상태 가치를 올린다**. 나빠 보이면 내린다.

## 핵심 F# 타입 (TICT-02)

```fsharp
{{#include ../../../TicTacToe/src/TicTacToe/Domain.fs}}
```

## 게임 규칙 엔진 (TICT-01)

```fsharp
{{#include ../../../TicTacToe/src/TicTacToe/Rules.fs}}
```

## TD Learning 에이전트 (TICT-04)

```fsharp
{{#include ../../../TicTacToe/src/TicTacToe/Agent.fs}}
```

## 자가 대국 학습 루프 (TICT-05)

```fsharp
{{#include ../../../TicTacToe/src/TicTacToe/Training.fs}}
```

## 학습 검증: 승률 > 90%

10만 판 자가 대국 후, 학습된 에이전트를 완전 랜덤 상대와 1,000판 대결시킨다.

```
FsCheck 보드 불변 조건 테스트:
  ✓ 빈칸 수는 합법적인 이동 1회 후 1 감소한다
  ✓ 플레이어는 교대한다: X 이후 O 차례
  ✓ 합법 수는 모두 [0, 8] 범위 내에 있다
  ✓ applyMove는 대상 칸만 변경하고 나머지는 유지한다
  ✓ 초기 보드는 9개의 빈 칸을 가진다

Expecto 수렴 테스트:
  ✓ TD 에이전트가 100k 자가 대국 후 랜덤 상대 승률 > 90%
```

## 학습 곡선 관찰

Serilog가 1,000판마다 현재 승률을 출력한다:

```
[12:34:56 INF] Episode=1000 WinRate=61.2%
[12:34:57 INF] Episode=2000 WinRate=72.5%
...
[12:35:10 INF] Episode=50000 WinRate=91.8%
[12:35:20 INF] Episode=100000 WinRate=93.4%
```

처음에는 무작위에 가깝다가, 점차 전략적 수가 쌓이며 수렴한다.

## Phase 3 예고: 왜 상태 공간이 문제인가?

틱택토의 상태 수는 ~5,478개 — `Map<Board, float>` 하나에 충분히 들어간다.

하지만 Connect Four는? 6×7 보드, 최대 42수. 가능한 상태 수는 4조(4×10¹²)를 넘는다.
이 크기의 가치 테이블은 메모리에 들어가지 않는다.

3장에서는 이 문제를 두 가지 방향으로 접근한다:
1. **Q-Learning + 특징 추출**: 상태 전체 대신 보드의 특징(연속 돌 수 등)을 사용
2. **Minimax Alpha-Beta**: 가치 함수 없이 게임 트리 탐색으로 최선 수 계산
```

**mdBook 빌드 확인:**
```bash
# repo root에서
mdbook build tutorial/
```
오류 없이 빌드 성공해야 한다.

빌드 실패 시:
- "file not found" → {{#include}} 경로 재확인
  - `ls tutorial/../TicTacToe/src/TicTacToe/Domain.fs` 로 경로 검증
  - README.md의 위치에서 정확히 3단계 위로 이동해야 repo root 도달
- 중첩 코드블록 문제: 백틱 3개 안에 백틱 3개가 있으면 mdBook이 오류 발생
  - 이 경우 {{#include}} 코드블록은 ```fsharp ... ``` 사용 (정상)
  - 직접 작성한 예시 코드가 중첩되지 않도록 확인
  </action>
  <verify>
```bash
cd /Users/ohama/vibe-coding/reinforcement-tutorial
mdbook build tutorial/
echo "mdbook build exit code: $?"
```
"Finished" 메시지와 exit code 0 확인. "file not found" 또는 오류 없음.
  </verify>
  <done>
- tutorial/src/02-tictactoe/README.md: 한국어 챕터, {{#include}} 4개 (Domain.fs, Rules.fs, Agent.fs, Training.fs)
- Phase 1 Bandit 한계 → MDP 필요성 설명 포함
- MDP 개념표, TD(0) 공식, 학습 곡선 예시, Phase 3 예고 포함
- `mdbook build tutorial/` 성공 (exit code 0, 오류 없음)
  </done>
</task>

</tasks>

<verification>
```bash
# 1. 전체 빌드 + 테스트
cd /Users/ohama/vibe-coding/reinforcement-tutorial/TicTacToe
dotnet build TicTacToe.sln && dotnet test TicTacToe.sln

# 2. mdBook 빌드
cd /Users/ohama/vibe-coding/reinforcement-tutorial
mdbook build tutorial/

# 3. {{#include}} 경로 검증
grep -r "{{#include" tutorial/src/02-tictactoe/README.md

# 4. Program.fs에서 Serilog 로그 구조 확인
grep "Episode=" TicTacToe/src/TicTacToe.Console/Program.fs
```
- `dotnet test` 전체 통과 (실패 0)
- `mdbook build` 성공 (오류 없음)
- Program.fs에 `Log.Information("Episode={Episode} WinRate=..."` 패턴 존재
</verification>

<success_criteria>
1. Program.fs: Serilog (Console + File sink), trainAgent 100k 호출, history 로깅, runHumanVsAI 루프
2. 사람 vs AI: 1-9 입력 처리, 이미 둔 자리/범위 외 입력 재시도, AI epsilon=0 탐욕 플레이
3. tutorial/src/02-tictactoe/README.md: 한국어 완성, {{#include}} 4개 파일 인클루드
4. Phase 1 Bandit 한계 → MDP 필요성 설명 포함 (TUTR-05)
5. `mdbook build tutorial/` 성공 — {{#include}} 경로 오류 없음
6. `dotnet build TicTacToe.sln` 성공

Phase 2 완료 기준:
- `dotnet test` → FsCheck 5개 + Expecto 3개 통과
- `dotnet run --project src/TicTacToe.Console` → Serilog 학습 로그 출력 후 사람 vs AI 시작
- `mdbook build tutorial/` → 02-tictactoe 챕터 빌드 성공
</success_criteria>

<output>
완료 후 `.planning/phases/02-tictactoe-td-learning/02-03-SUMMARY.md` 생성
</output>
