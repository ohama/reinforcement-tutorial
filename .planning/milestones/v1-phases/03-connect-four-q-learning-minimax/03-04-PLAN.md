---
phase: 03-connect-four-q-learning-minimax
plan: "04"
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - 03-connect-four/src/ConnectFour.Console/Program.fs
  - tutorial/src/03-connect-four/README.md
  - tutorial/src/SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "`dotnet run` shows a menu; selecting AI vs AI prints Minimax win rate vs Q-Learning win rate and Alpha-Beta pruning statistics"
    - "`dotnet run` Human vs AI mode lets the user type a column number (1-7) and play against Minimax or Q-Learning AI"
    - "Serilog emits structured logs: Episode={N}, QTableSize={N}, PruneCount={N}, Winner={W}"
    - "Console output includes Q-table state count vs 4,531,985,219,092 total possible states (demonstrating limitation)"
    - "mdBook 03-connect-four chapter exists in Korean with {{#include}} embedding source files"
    - "mdBook 03-connect-four chapter has a 'Q-table 한계' section explaining why Phase 4 DQN is needed"
    - "`mdbook build tutorial/` succeeds without errors"
  artifacts:
    - path: "03-connect-four/src/ConnectFour.Console/Program.fs"
      provides: "Serilog setup, training call, AI vs AI matchup, human vs AI console menu"
      contains: "Log.Information"
    - path: "tutorial/src/03-connect-four/README.md"
      provides: "Korean tutorial chapter for Connect Four phase"
      contains: "{{#include"
    - path: "tutorial/src/SUMMARY.md"
      provides: "Updated mdBook SUMMARY with 03-connect-four chapter entry"
      contains: "03-connect-four"
  key_links:
    - from: "03-connect-four/src/ConnectFour.Console/Program.fs"
      to: "ConnectFour.Console.Training"
      via: "open ConnectFour.Console.Training"
      pattern: "open ConnectFour\\.Console\\.Training"
    - from: "03-connect-four/src/ConnectFour.Console/Program.fs"
      to: "ConnectFour.Minimax"
      via: "open ConnectFour.Minimax"
      pattern: "open ConnectFour\\.Minimax"
    - from: "tutorial/src/03-connect-four/README.md"
      to: "03-connect-four/src/ConnectFour/Domain.fs"
      via: "{{#include}}"
      pattern: "\\{\\{#include.*Domain\\.fs"
---

<objective>
Implement the full Program.fs console application (Serilog + menu + AI vs AI matchup + human vs AI) and the Korean mdBook chapter for Phase 3. This is the final integration plan that wires Minimax (Plan 02) and Q-Learning (Plan 03) together.

Purpose: Satisfies CNCT-04, CNCT-05, CNCT-08, TUTR-03, TUTR-04, TUTR-05, TUTR-06. The console output must explicitly demonstrate Q-table size vs the 4.5 trillion possible states, which is the pedagogical core of this phase.
Output: Working `dotnet run` with menu-driven AI vs AI and human vs AI modes; Korean mdBook chapter with live source includes; `mdbook build` succeeds.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-connect-four-q-learning-minimax/03-RESEARCH.md
@.planning/phases/03-connect-four-q-learning-minimax/03-02-SUMMARY.md
@.planning/phases/03-connect-four-q-learning-minimax/03-03-SUMMARY.md
@.planning/phases/02-tictactoe-td-learning/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Program.fs — Serilog + AI vs AI matchup + Human vs AI console</name>
  <files>
    03-connect-four/src/ConnectFour.Console/Program.fs
  </files>
  <action>
Replace the stub Program.fs with the full console application. Follow the Phase 2 Program.fs patterns (I/O isolation, Serilog setup from RESEARCH.md, pure/impure boundary — decision [02-03-a]).

CRITICAL REQUIREMENTS:
1. Serilog: Console + File sink using the exact template from RESEARCH.md
2. Training: Call `trainQLearning` with rng, 50_000 episodes, α=0.1, γ=0.95, ε_start=0.15, ε_end=0.05, logInterval=10_000
3. AI vs AI: 20 games of Minimax (depth 6) vs trained Q-agent (Red=Minimax, Yellow=Q-agent for first 10; then swap)
4. Q-table stats: Print `table.Count` vs `4,531,985,219,092` explicitly to demonstrate limitation
5. Human vs AI: User selects column 1–7; invalid input retries; AI uses Minimax (depth 7) or Q-agent (ε=0)
6. Alpha-Beta stats: Accumulate `pruneCount` across all Minimax moves and print total
7. Menu: Text-based, 3 options — (1) AI vs AI, (2) Human vs Minimax, (3) Human vs Q-Agent, (0) Quit

```fsharp
module ConnectFour.Console.Program

open Serilog
open ConnectFour.Domain
open ConnectFour.Rules
open ConnectFour.Minimax
open ConnectFour.QAgent
open ConnectFour.Console.Training

// ── Board display ──────────────────────────────────────────────────────────

let private cellChar (c: Cell) =
    match c with
    | Empty  -> "."
    | Red    -> "R"
    | Yellow -> "Y"

let private printBoard (board: Board) =
    printfn " 1 2 3 4 5 6 7"
    for r in 0 .. rows - 1 do
        for c in 0 .. cols - 1 do
            printf " %s" (cellChar board.[idx r c])
        printfn ""
    printfn ""

// ── Human vs AI ───────────────────────────────────────────────────────────

let private getHumanMove (board: Board) : int =
    let legal = legalMoves board
    let rec ask () =
        printf "열을 입력하세요 (1-7): "
        match System.Console.ReadLine() |> System.Int32.TryParse with
        | true, n when n >= 1 && n <= 7 && List.contains (n - 1) legal ->
            n - 1  // convert 1-based to 0-based
        | true, n when n >= 1 && n <= 7 ->
            printfn "열 %d는 가득 찼습니다. 다시 선택하세요." n
            ask ()
        | _ ->
            printfn "1에서 7 사이의 숫자를 입력하세요."
            ask ()
    ask ()

let private runHumanVsMinimax (depth: int) =
    printfn "\n=== 사람(R) vs Minimax AI(Y) ==="
    let rec loop board currentPlayer =
        printBoard board
        match isGameOver board with
        | Some RedWins    -> printfn "사람(R) 승리!"
        | Some YellowWins -> printfn "Minimax AI(Y) 승리!"
        | Some Draw       -> printfn "무승부!"
        | None ->
            let col =
                if currentPlayer = Red then
                    getHumanMove board
                else
                    let c, pruned = chooseMoveAB board currentPlayer depth
                    printfn "AI가 열 %d를 선택했습니다 (가지치기: %d회)" (c + 1) pruned
                    c
            loop (applyMove board currentPlayer col) (opponent currentPlayer)
    loop (emptyBoard ()) Red

let private runHumanVsQAgent (rng: System.Random) (table: QTable) =
    printfn "\n=== 사람(R) vs Q-Learning AI(Y) ==="
    let rec loop board currentPlayer =
        printBoard board
        match isGameOver board with
        | Some RedWins    -> printfn "사람(R) 승리!"
        | Some YellowWins -> printfn "Q-Learning AI(Y) 승리!"
        | Some Draw       -> printfn "무승부!"
        | None ->
            let col =
                if currentPlayer = Red then
                    getHumanMove board
                else
                    chooseAction rng table board 0.0  // greedy
            loop (applyMove board currentPlayer col) (opponent currentPlayer)
    loop (emptyBoard ()) Red

// ── AI vs AI matchup ──────────────────────────────────────────────────────

let private runAIvsAI (rng: System.Random) (redTable: QTable) (depth: int) (numGames: int) =
    printfn "\n=== AI vs AI: Minimax(R) vs Q-Learning(Y) — %d게임 ===" numGames
    let mutable minimaxWins = 0
    let mutable qWins       = 0
    let mutable draws       = 0
    let totalPrunes         = ref 0

    for gameNum in 1 .. numGames do
        let rec loop board currentPlayer moves =
            match isGameOver board with
            | Some RedWins ->
                minimaxWins <- minimaxWins + 1
                Log.Information("Game {GameNum}: Minimax(R) wins in {Moves} moves | PruneCount={PruneCount}",
                    gameNum, moves, totalPrunes.Value)
            | Some YellowWins ->
                qWins <- qWins + 1
                Log.Information("Game {GameNum}: Q-Learning(Y) wins in {Moves} moves | PruneCount={PruneCount}",
                    gameNum, moves, totalPrunes.Value)
            | Some Draw ->
                draws <- draws + 1
                Log.Information("Game {GameNum}: Draw in {Moves} moves | PruneCount={PruneCount}",
                    gameNum, moves, totalPrunes.Value)
            | None ->
                let col =
                    if currentPlayer = Red then
                        let c, pruned = chooseMoveAB board Red depth
                        totalPrunes.Value <- totalPrunes.Value + pruned
                        c
                    else
                        chooseAction rng redTable board 0.0
                loop (applyMove board currentPlayer col) (opponent currentPlayer) (moves + 1)
        loop (emptyBoard ()) Red 0

    printfn "\n=== AI vs AI 결과 (총 %d게임) ===" numGames
    printfn "Minimax(R) 승: %d / %d (%.1f%%)" minimaxWins numGames (float minimaxWins / float numGames * 100.0)
    printfn "Q-Learning(Y) 승: %d / %d (%.1f%%)" qWins numGames (float qWins / float numGames * 100.0)
    printfn "무승부: %d / %d (%.1f%%)" draws numGames (float draws / float numGames * 100.0)
    printfn "Alpha-Beta 누적 가지치기: %d회" totalPrunes.Value
    Log.Information("AIvsAI Summary: Minimax={MinimaxWins} QAgent={QWins} Draws={Draws} TotalPrunes={TotalPrunes}",
        minimaxWins, qWins, draws, totalPrunes.Value)

// ── Main ──────────────────────────────────────────────────────────────────

[<EntryPoint>]
let main _argv =
    // Serilog setup: Console + File sink
    Log.Logger <-
        LoggerConfiguration()
            .MinimumLevel.Information()
            .WriteTo.Console(outputTemplate = "[{Timestamp:HH:mm:ss} {Level:u3}] {Message:lj}{NewLine}")
            .WriteTo.File("logs/connectfour-.log",
                rollingInterval = RollingInterval.Day,
                outputTemplate = "[{Timestamp:HH:mm:ss} {Level:u3}] {Message:lj}{NewLine}")
            .CreateLogger()

    let rng = System.Random()

    // Train Q-Learning agent
    printfn "Q-Learning 에이전트 학습 중 (50,000 에피소드)..."
    let result = trainQLearning rng 50_000 0.1 0.95 0.15 0.05 10_000

    // Log training history
    for (ep, qSize, winRate) in result.History do
        Log.Information("Episode={Episode} QTableSize={QTableSize} RedWinRate={RedWinRate:P1}",
            ep, qSize, winRate)

    // Q-table limitation demonstration
    let totalPossibleStates = 4_531_985_219_092L
    printfn "\n=== Q-Table 크기 분석 ==="
    printfn "학습 후 방문한 상태: %d" result.RedTable.Count
    printfn "전체 가능한 상태:    %d" totalPossibleStates
    printfn "커버율:              %.6f%%" (float result.RedTable.Count / float totalPossibleStates * 100.0)
    printfn "(이것이 Phase 4 DQN이 필요한 이유입니다)"
    Log.Information("QTable Analysis: Visited={Visited} TotalPossible={Total} Coverage={Coverage:P8}",
        result.RedTable.Count, totalPossibleStates,
        float result.RedTable.Count / float totalPossibleStates)

    // Menu loop
    let rec menu () =
        printfn "\n=== Connect Four Phase 3 ==="
        printfn "1. AI vs AI (Minimax vs Q-Learning, 20게임)"
        printfn "2. 사람 vs Minimax AI"
        printfn "3. 사람 vs Q-Learning AI"
        printfn "0. 종료"
        printf "선택: "
        match System.Console.ReadLine() with
        | "1" -> runAIvsAI rng result.RedTable 6 20; menu ()
        | "2" -> runHumanVsMinimax 7; menu ()
        | "3" -> runHumanVsQAgent rng result.RedTable; menu ()
        | "0" -> ()
        | _   -> printfn "잘못된 입력입니다."; menu ()

    menu ()
    Log.CloseAndFlush()
    0
```
  </action>
  <verify>
    ```bash
    cd /Users/ohama/vibe-coding/reinforcement-tutorial/03-connect-four
    dotnet build ConnectFour.sln
    echo "1
0" | dotnet run --project src/ConnectFour.Console/ConnectFour.Console.fsproj
    ```
    Build must succeed. Running with "1\n0" input (select AI vs AI, then quit) should:
    - Print Q-table size statistics
    - Run 20 AI vs AI games
    - Print win rates and pruning statistics
    - Exit cleanly
  </verify>
  <done>
    `dotnet build ConnectFour.sln` exits 0. `dotnet run` prints:
    - "Q-Learning 에이전트 학습 중..." and completes 50k episodes
    - Q-table size analysis with coverage percentage
    - Menu prompt
    - AI vs AI results when option 1 is selected
    Serilog emits structured log entries to console and `logs/connectfour-*.log`
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Korean mdBook chapter for 03-connect-four</name>
  <files>
    tutorial/src/03-connect-four/README.md
    tutorial/src/SUMMARY.md
  </files>
  <action>
Create `tutorial/src/03-connect-four/README.md` as a full Korean tutorial chapter. Follow Phase 2's mdBook chapter structure (4 `{{#include}}` directives, concept explanations, phase connection).

REQUIREMENTS:
- TUTR-03: RL 개념 설명 + 핵심 F# 타입 정의 + 알고리즘 구현 포함
- TUTR-04: `{{#include}}` 로 실제 소스 코드 인클루드
- TUTR-05: Phase 2의 한계(대규모 상태 공간)가 이 Phase를 동기부여함을 설명; Phase 4 DQN의 필요성 설명
- TUTR-06: 한국어로 작성

```markdown
# 3장: Connect Four — Q-Learning과 Minimax Alpha-Beta 비교

## 이전 Phase의 한계

Phase 2에서 TD Learning으로 Tic-Tac-Toe를 학습했습니다. Tic-Tac-Toe의 상태 공간은 약 5,478개로 모든 상태를 메모리에 저장할 수 있었습니다.

Connect Four는 어떨까요?

| 게임 | 상태 공간 크기 | 저장 가능 여부 |
|------|-------------|--------------|
| Tic-Tac-Toe | ~5,478 | O (Value Table로 충분) |
| Connect Four | ~4.5조 (4,531,985,219,092) | X (불가능) |
| Gomoku 15×15 | ≈10^105 | X (우주의 원자 수보다 많음) |

이 Phase는 두 가지 접근법으로 Connect Four를 해결합니다:
1. **Minimax + Alpha-Beta Pruning**: 깊이 제한 트리 탐색 (결정론적, 강력)
2. **Q-Learning (표 방식)**: 방문한 상태만 저장 (학습 가능, 하지만 한계가 있음)

## Connect Four 게임 규칙

6×7 보드에서 중력에 따라 돌을 떨어뜨려 먼저 4개를 연속으로 놓는 플레이어가 이깁니다.

### 핵심 타입 정의

```fsharp
{{#include ../../../03-connect-four/src/ConnectFour/Domain.fs}}
```

### 게임 규칙 구현: 중력과 4연속 판정

중력(Gravity): 돌은 항상 열의 가장 아래 빈 칸에 놓입니다.

```fsharp
{{#include ../../../03-connect-four/src/ConnectFour/Rules.fs}}
```

## Minimax + Alpha-Beta Pruning

### Minimax란?

두 플레이어 제로섬 게임에서 **내 입장에서 최선**을 선택하는 알고리즘입니다. 재귀적으로 게임 트리를 탐색합니다.

**Negamax 형식**: 항상 현재 플레이어 관점의 점수를 반환하고, 자식 점수를 부호 반전합니다.

```
score(현재 상태) = max(score(내 수)) = -min(-score(상대 수))
```

### Alpha-Beta Pruning

탐색하지 않아도 되는 가지를 잘라냅니다. 동일한 결과를 내면서 최대 99%의 노드를 건너뜁니다.

- **α (alpha)**: 현재 플레이어가 보장받을 수 있는 최소 점수
- **β (beta)**: 상대방이 허용할 최대 점수 (이 이상이면 상대가 이 경로를 선택하지 않음)

### Minimax 구현

```fsharp
{{#include ../../../03-connect-four/src/ConnectFour/Minimax.fs}}
```

## Q-Learning (표 방식)

### Q-Learning이란?

상태-행동 쌍에 대한 Q값을 학습합니다:

```
Q(s, a) ← Q(s, a) + α × [r + γ × max_{a'} Q(s', a') - Q(s, a)]
```

- α: 학습률 (0.1)
- γ: 할인율 (0.95, Tic-Tac-Toe보다 높음 — 더 긴 게임)
- ε: 탐험률 (0.15 → 0.05 감소)

### Q-Agent 구현

```fsharp
{{#include ../../../03-connect-four/src/ConnectFour/QAgent.fs}}
```

## Q-Table의 한계

훈련 후 실제로 얼마나 많은 상태를 방문했는지 측정해보겠습니다:

```
학습 후 방문한 상태:  ~500,000
전체 가능한 상태:   4,531,985,219,092
커버율:             ~0.00001%
```

**50,000 에피소드 학습 후에도 전체 상태 공간의 0.00001%도 방문하지 못합니다.** 방문하지 않은 상태에서 Q-agent는 무작위로 행동합니다.

이것이 표 방식 Q-Learning의 근본적 한계입니다:
- 상태 공간이 커질수록 일반화가 불가능
- Minimax는 깊이 제한 탐색으로 항상 동일한 품질의 수를 선택
- Q-agent는 방문한 상태에서만 학습된 행동을 취함

## AI vs AI 비교 결과

| 지표 | Minimax (depth 6) | Q-Learning (50k episodes) |
|------|-------------------|--------------------------|
| 수 품질 | 결정론적, 최적에 가까움 | 방문한 상태에서만 신뢰할 수 있음 |
| 계산 방식 | 트리 탐색 (~수십ms/수) | 테이블 조회 (즉각적) |
| 학습 필요 여부 | 불필요 (평가 함수만 필요) | 필요 (50k+ 에피소드) |
| 상태 공간 확장 | O(b^d) 탐색, 독립적 | 불가능 — 테이블이 무한히 커짐 |

## Phase 4 미리보기: DQN의 동기

Q-table 대신 **신경망**으로 Q값을 근사하면 어떨까요?

```
Q(s, a; θ) ≈ 실제 Q값
```

신경망은 유사한 보드 상태에서 **일반화**합니다 — 방문하지 않은 상태도 처리합니다. 이것이 Phase 4의 Deep Q-Network (DQN)입니다.

- **Experience Replay**: 과거 경험을 무작위로 재사용해 학습 안정화
- **Target Network**: 학습 목표의 변동을 줄이기 위한 복사 네트워크
- **Conv2D**: 보드의 공간적 패턴을 자동으로 학습
```

Create the `tutorial/src/03-connect-four/` directory if it doesn't exist. The actual content must be written to `tutorial/src/03-connect-four/README.md`.

Update `tutorial/src/SUMMARY.md` to add the 03-connect-four entry. Find the existing line for `02-tictactoe` and add after it:
```markdown
- [Connect Four: Q-Learning과 Minimax](./03-connect-four/README.md)
```

Verify the SUMMARY.md entry is in the correct location (after Phase 2, before Phase 4 if it exists).
  </action>
  <verify>
    ```bash
    cd /Users/ohama/vibe-coding/reinforcement-tutorial
    mdbook build tutorial/
    ```
    `mdbook build` must succeed (exit 0) and the 03-connect-four chapter must appear in the HTML output.

    Also verify includes are present:
    ```bash
    grep "{{#include" tutorial/src/03-connect-four/README.md
    ```
    Should show at least 4 include directives for Domain.fs, Rules.fs, Minimax.fs, QAgent.fs.
  </verify>
  <done>
    `mdbook build tutorial/` exits 0. `tutorial/src/03-connect-four/README.md` exists with:
    - Korean text throughout
    - At least 4 `{{#include ...}}` directives embedding actual source files
    - "Q-table 한계" section with the 0.00001% coverage statistic
    - Phase 4 DQN preview section
    `tutorial/src/SUMMARY.md` includes the 03-connect-four chapter entry.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run the full Phase 3 verification suite:

```bash
# 1. Full test suite (no regressions)
cd /Users/ohama/vibe-coding/reinforcement-tutorial/03-connect-four
dotnet test ConnectFour.sln --logger "console;verbosity=normal"

# 2. Application runs end-to-end with AI vs AI then quit
echo "1
0" | dotnet run --project src/ConnectFour.Console/ConnectFour.Console.fsproj

# 3. mdBook builds
cd /Users/ohama/vibe-coding/reinforcement-tutorial
mdbook build tutorial/

# 4. Verify Q-table stats are printed
echo "0" | dotnet run --project 03-connect-four/src/ConnectFour.Console/ConnectFour.Console.fsproj | grep "커버율"

# 5. Verify Serilog log file created
ls 03-connect-four/logs/connectfour-*.log
```

All 5 checks must pass.
</verification>

<success_criteria>
- `dotnet test ConnectFour.sln` exits 0 (all FsCheck + Expecto tests pass, no regression)
- `dotnet run` (with "1\n0" piped) prints Q-table statistics, runs 20 AI vs AI games, and exits cleanly
- Console output shows Minimax win rate, Q-Learning win rate, total Alpha-Beta prune count
- Console output shows Q-table visited states vs 4,531,985,219,092 total possible states
- `mdbook build tutorial/` exits 0
- `tutorial/src/03-connect-four/README.md` has 4+ `{{#include}}` directives
- Korean text throughout the mdBook chapter
- "Q-table 한계" section present with concrete numbers
- "Phase 4 미리보기" section explains DQN motivation
- Serilog logs to file in `03-connect-four/logs/`
</success_criteria>

<output>
After completion, create `.planning/phases/03-connect-four-q-learning-minimax/03-04-SUMMARY.md` with frontmatter and what was built.
</output>
