---
phase: 02-tictactoe-td-learning
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - TicTacToe/src/TicTacToe/TicTacToe.fsproj
  - TicTacToe/src/TicTacToe/Agent.fs
  - TicTacToe/src/TicTacToe/Training.fs
  - TicTacToe/tests/TicTacToe.Tests/TicTacToe.Tests.fsproj
  - TicTacToe/tests/TicTacToe.Tests/ConvergenceTests.fs
  - TicTacToe/tests/TicTacToe.Tests/Main.fs
autonomous: true

must_haves:
  truths:
    - "dotnet test TicTacToe.sln 실행 시 모든 FsCheck + Expecto 테스트가 통과한다"
    - "TD 에이전트가 100k 자가 대국 학습 후 랜덤 상대 대비 승률 > 90%를 달성한다"
    - "randomAgent가 항상 합법적인 수(0-8)를 반환한다"
    - "tdUpdate가 V(s) += alpha * (V(s') - V(s)) 공식을 구현한다"
    - "trainAgent가 ValueTable과 win-rate history를 반환한다"
  artifacts:
    - path: "TicTacToe/src/TicTacToe/Agent.fs"
      provides: "randomAgent, tdAgent (epsilon-greedy), tdUpdate"
      min_lines: 35
    - path: "TicTacToe/src/TicTacToe/Training.fs"
      provides: "playEpisode, trainAgent, winRateVsRandom"
      min_lines: 50
    - path: "TicTacToe/tests/TicTacToe.Tests/ConvergenceTests.fs"
      provides: "Expecto convergence test: >90% win rate after 100k games"
      contains: "0.90"
  key_links:
    - from: "TicTacToe/src/TicTacToe/Agent.fs"
      to: "TicTacToe/src/TicTacToe/Rules.fs"
      via: "open TicTacToe.Rules"
      pattern: "open TicTacToe\\.Rules"
    - from: "TicTacToe/src/TicTacToe/Training.fs"
      to: "TicTacToe/src/TicTacToe/Agent.fs"
      via: "open TicTacToe.Agent"
      pattern: "open TicTacToe\\.Agent"
    - from: "TicTacToe/tests/TicTacToe.Tests/ConvergenceTests.fs"
      to: "TicTacToe/src/TicTacToe/Training.fs"
      via: "trainAgent + winRateVsRandom"
      pattern: "trainAgent.*winRateVsRandom"
---

<objective>
TD(0) 에이전트 + 자가 대국 학습 루프 구현 + Expecto 수렴 테스트

Purpose: Agent.fs에서 ε-greedy TD 에이전트와 TD(0) 업데이트를 구현하고, Training.fs에서 100k 자가 대국 학습 루프를 구현한다. Expecto 테스트로 >90% 승률 수렴을 검증한다.
Output: Agent.fs, Training.fs — 학습 가능한 ValueTable. ConvergenceTests.fs — Expecto >90% win rate 테스트 통과.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-tictactoe-td-learning/02-RESEARCH.md
@.planning/phases/02-tictactoe-td-learning/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agent.fs + Training.fs 구현 (TD(0) 학습 코어)</name>
  <files>
    TicTacToe/src/TicTacToe/TicTacToe.fsproj
    TicTacToe/src/TicTacToe/Agent.fs
    TicTacToe/src/TicTacToe/Training.fs
  </files>
  <action>
**Step 1 — TicTacToe.fsproj 파일 순서 업데이트:**
`src/TicTacToe/TicTacToe.fsproj`의 `<ItemGroup>` Compile 항목을 다음 순서로 업데이트한다:
```xml
<Compile Include="Domain.fs" />
<Compile Include="Rules.fs" />
<Compile Include="Agent.fs" />
<Compile Include="Training.fs" />
```
F# 컴파일 순서는 파일 순서 그대로다. Agent.fs는 Rules.fs에 의존하고, Training.fs는 Agent.fs에 의존한다. 순서 위반 시 "not defined" 오류.

**Step 2 — Agent.fs 작성** (`src/TicTacToe/Agent.fs`):

핵심 개념:
- ValueTable은 X의 관점에서 저장 (V(s) = X가 이 보드에서 이길 확률)
- X는 V를 최대화, O는 V를 최소화 (O의 최선 수 = 후계 상태 중 V가 가장 낮은 것)
- 비종단 상태의 기본값 = 0.5 (Map.tryFind ... |> Option.defaultValue 0.5)
- 평가 시 epsilon = 0.0 (순수 탐욕), 학습 시 epsilon > 0 (탐험)

```fsharp
module TicTacToe.Agent

open TicTacToe.Domain
open TicTacToe.Rules

/// TD(0) 업데이트: V(s) <- V(s) + alpha * (V(s') - V(s))
/// alpha: 학습률 (전형적으로 0.1-0.3)
/// 모든 값은 X의 관점 (V=1.0: X 확실 승, V=0.0: O 확실 승, V=0.5: 불명/무승부)
let tdUpdate (alpha: float) (vTable: ValueTable) (state: Board) (nextState: Board) : ValueTable =
    let vCurrent = Map.tryFind state vTable |> Option.defaultValue 0.5
    let vNext    = Map.tryFind nextState vTable |> Option.defaultValue 0.5
    let vNew     = vCurrent + alpha * (vNext - vCurrent)
    Map.add state vNew vTable

/// 랜덤 에이전트 (TICT-03): 합법 수 중 무작위 선택
let randomAgent (rng: System.Random) (state: GameState) : int =
    let moves = legalMoves state.Board
    moves.[rng.Next(moves.Length)]

/// TD 에이전트 (TICT-04): epsilon-greedy 수 선택
/// X는 V(후계)를 최대화, O는 V(후계)를 최소화
/// epsilon 확률로 무작위, 그 외에는 탐욕적 선택
let tdAgent (rng: System.Random) (epsilon: float) (vTable: ValueTable) (state: GameState) : int =
    let moves = legalMoves state.Board
    if rng.NextDouble() < epsilon then
        moves.[rng.Next(moves.Length)]  // 탐험
    else
        let getBoardValue board =
            Map.tryFind board vTable |> Option.defaultValue 0.5
        let scoredMoves =
            moves
            |> List.map (fun move ->
                let nextBoard = applyMove state.Board state.CurrentPlayer move
                let v = getBoardValue nextBoard
                move, v)
        match state.CurrentPlayer with
        | X -> scoredMoves |> List.maxBy snd |> fst  // X: V 최대화
        | O -> scoredMoves |> List.minBy snd |> fst  // O: V 최소화
        | Empty -> failwith "tdAgent: Empty는 유효한 플레이어가 아닙니다"
```

**Step 3 — Training.fs 작성** (`src/TicTacToe/Training.fs`):

핵심 설계 결정:
- playEpisode: 두 TD 에이전트가 동일 ValueTable을 공유하며 자가 대국
- prevBoard를 추적하여 현재 플레이어의 이전 보드 상태를 백업
- 종단 시 terminal 값 (1.0/0.0/0.5) 설정 후 마지막 백업 적용
- trainAgent: 누적 wins를 추적하여 logInterval마다 win rate history 기록
- winRateVsRandom: TD 에이전트(X, epsilon=0) vs 랜덤 에이전트(O) 평가

```fsharp
module TicTacToe.Training

open TicTacToe.Domain
open TicTacToe.Rules
open TicTacToe.Agent

/// 한 에피소드 자가 대국 실행.
/// 두 TD 에이전트가 동일 ValueTable(공유)으로 경쟁.
/// 반환: (업데이트된 ValueTable, 게임 결과)
let playEpisode
    (rng: System.Random)
    (alpha: float)
    (epsilon: float)
    (vTable: ValueTable) : ValueTable * GameResult =

    let rec loop (state: GameState) (vTable: ValueTable) (prevBoard: Board option) =
        match isGameOver state.Board with
        | Some result ->
            // 종단 상태: 최종 값 고정 + 마지막 백업
            let terminalValue =
                match result with
                | XWins -> 1.0
                | OWins -> 0.0
                | Draw  -> 0.5
            let vTable' = Map.add state.Board terminalValue vTable
            let vTable'' =
                match prevBoard with
                | Some prev -> tdUpdate alpha vTable' prev state.Board
                | None -> vTable'
            vTable'', result
        | None ->
            let move = tdAgent rng epsilon vTable state
            let nextBoard = applyMove state.Board state.CurrentPlayer move
            let nextState = { Board = nextBoard; CurrentPlayer = otherPlayer state.CurrentPlayer }
            // TD 백업: 현재 플레이어의 이전 보드를 현재 보드 방향으로 업데이트
            let vTable' =
                match prevBoard with
                | Some prev -> tdUpdate alpha vTable prev state.Board
                | None -> vTable
            loop nextState vTable' (Some state.Board)

    loop (initialState ()) vTable None

/// N 에피소드 자가 대국 학습 (TICT-05).
/// 반환: (최종 ValueTable, win rate history [(episode, xWinRate)])
/// logInterval마다 현재까지의 X 승률을 history에 기록
let trainAgent
    (rng: System.Random)
    (episodes: int)
    (alpha: float)
    (epsilon: float)
    (logInterval: int) : ValueTable * (int * float) list =

    let rec loop ep vTable wins history =
        if ep > episodes then
            vTable, List.rev history
        else
            let vTable', result = playEpisode rng alpha epsilon vTable
            let wins' = if result = XWins then wins + 1 else wins
            let history' =
                if ep % logInterval = 0 && ep > 0 then
                    let rate = float wins' / float ep
                    (ep, rate) :: history
                else history
            loop (ep + 1) vTable' wins' history'

    loop 1 Map.empty 0 []

/// TD 에이전트(X, epsilon=0) vs 랜덤 에이전트(O) 승률 측정 (TICT-08)
/// games 판 중 X 승리 비율 반환
let winRateVsRandom
    (rng: System.Random)
    (vTable: ValueTable)
    (games: int) : float =
    let wins =
        [ 1..games ]
        |> List.sumBy (fun _ ->
            let rec play state =
                match isGameOver state.Board with
                | Some XWins -> 1
                | Some _ -> 0
                | None ->
                    let move =
                        if state.CurrentPlayer = X then
                            tdAgent rng 0.0 vTable state  // 평가 시 epsilon=0 (순수 탐욕)
                        else
                            randomAgent rng state
                    let next = {
                        Board = applyMove state.Board state.CurrentPlayer move
                        CurrentPlayer = otherPlayer state.CurrentPlayer
                    }
                    play next
            play (initialState ()))
    float wins / float games
```

**Step 4 — 빌드 확인:**
```bash
# TicTacToe/ 디렉터리에서
dotnet build TicTacToe.sln
```
오류 없이 빌드 성공해야 한다.
  </action>
  <verify>
```bash
cd /Users/ohama/vibe-coding/reinforcement-tutorial/TicTacToe
dotnet build TicTacToe.sln
```
"Build succeeded" 메시지 확인. 오류 0개.
  </verify>
  <done>
- Agent.fs: tdUpdate, randomAgent, tdAgent (epsilon-greedy, X 최대화/O 최소화)
- Training.fs: playEpisode (재귀 루프, prevBoard 추적), trainAgent (100k 에피소드), winRateVsRandom (epsilon=0)
- TicTacToe.fsproj 파일 순서: Domain.fs → Rules.fs → Agent.fs → Training.fs
- `dotnet build TicTacToe.sln` 성공
  </done>
</task>

<task type="auto">
  <name>Task 2: Expecto 수렴 테스트 추가 + dotnet test 전체 통과</name>
  <files>
    TicTacToe/tests/TicTacToe.Tests/TicTacToe.Tests.fsproj
    TicTacToe/tests/TicTacToe.Tests/ConvergenceTests.fs
    TicTacToe/tests/TicTacToe.Tests/Main.fs
  </files>
  <action>
**Step 1 — TicTacToe.Tests.fsproj에 ConvergenceTests.fs 추가:**
`tests/TicTacToe.Tests/TicTacToe.Tests.fsproj`의 `<ItemGroup>` Compile 항목을 다음으로 업데이트한다:
```xml
<Compile Include="PropertyTests.fs" />
<Compile Include="ConvergenceTests.fs" />
<Compile Include="Main.fs" />
```

**Step 2 — ConvergenceTests.fs 작성** (`tests/TicTacToe.Tests/ConvergenceTests.fs`):

핵심 주의사항:
- TICT-08: 100k 게임 후 랜덤 대비 승률 > 90% (testCase, 고정 seed)
- 평가는 epsilon=0 사용 (winRateVsRandom 내부에서 이미 처리됨)
- 100k 에피소드 실행 시간: F#에서 약 3-10초 — 테스트 타임아웃 걱정 불필요
- Expect.isGreaterThan의 인자 순서: (actual, expected, message)

```fsharp
module TicTacToe.Tests.ConvergenceTests

open Expecto
open TicTacToe.Domain
open TicTacToe.Training

[<Tests>]
let convergenceTests =
    testList "Expecto 수렴 테스트" [

        // TICT-08: 100k 에피소드 학습 후 랜덤 상대 승률 > 90%
        testCase "TD 에이전트가 100k 자가 대국 후 랜덤 상대 승률 > 90%" <| fun () ->
            let rng = System.Random(42)
            let vTable, _ = trainAgent rng 100_000 0.1 0.1 1_000
            let winRate = winRateVsRandom rng vTable 1_000
            Expect.isGreaterThan winRate 0.90
                $"승률 > 90%% 기대, 실제: {winRate * 100.0:F1}%%"

        // 랜덤 에이전트 기본 확인: 유효한 수 반환
        testCase "랜덤 에이전트는 항상 합법 수(0-8)를 반환한다" <| fun () ->
            let rng = System.Random(1)
            let state = initialState ()
            let move = TicTacToe.Agent.randomAgent rng state
            Expect.isTrue (move >= 0 && move <= 8) "수는 [0, 8] 범위 내여야 한다"

        // TD 에이전트 기본 확인: 비종단 보드에서 유효한 수 반환
        testCase "TD 에이전트는 비종단 보드에서 합법 수를 반환한다" <| fun () ->
            let rng = System.Random(42)
            let vTable, _ = trainAgent rng 1_000 0.1 0.1 100
            let state = initialState ()
            let move = TicTacToe.Agent.tdAgent rng 0.0 vTable state
            Expect.isTrue (move >= 0 && move <= 8) "TD 에이전트 수는 [0, 8] 범위 내여야 한다"

    ]
```

**Step 3 — Main.fs 업데이트** (`tests/TicTacToe.Tests/Main.fs`):
두 테스트 리스트를 모두 실행하도록 업데이트:
```fsharp
module TicTacToe.Tests.Main

open Expecto

[<EntryPoint>]
let main args =
    let allTests =
        testList "모든 TicTacToe 테스트" [
            propertyTests
            convergenceTests
        ]
    runTestsWithCLIArgs [] args allTests
```

**Step 4 — dotnet test 실행:**
```bash
# TicTacToe/ 디렉터리에서
dotnet test TicTacToe.sln --verbosity normal
```

예상 결과: FsCheck 5개 + Expecto 3개 = 총 8개 테스트 통과.
100k 에피소드 테스트는 수 초 소요 — 정상.

테스트 실패 진단:
- "승률 > 90% 기대" 실패 → alpha/epsilon 파라미터 또는 학습 로직 확인
  - 특히 prevBoard 백업 로직 확인: 현재 플레이어의 이전 보드를 백업해야 함
  - epsilon=0으로 평가하는지 winRateVsRandom 확인
- "not defined" 오류 → TicTacToe.Tests.fsproj Compile 순서 확인
  </action>
  <verify>
```bash
cd /Users/ohama/vibe-coding/reinforcement-tutorial/TicTacToe
dotnet test TicTacToe.sln --verbosity normal 2>&1 | tail -25
```
"Passed!" 메시지와 함께 Failed: 0, Passed: 8 (또는 FsCheck 반복 포함하여 더 많은 수) 확인.
  </verify>
  <done>
- ConvergenceTests.fs: 3개 Expecto testCase ([<Tests>] 어트리뷰트)
- 100k 자가 대국 후 TD 에이전트 승률 > 90% 테스트 통과
- Main.fs에서 propertyTests + convergenceTests 모두 실행
- `dotnet test TicTacToe.sln` — 전체 8개 이상 테스트 통과, 실패 0개
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/ohama/vibe-coding/reinforcement-tutorial/TicTacToe
dotnet build TicTacToe.sln && dotnet test TicTacToe.sln --verbosity normal
```
- 빌드 성공 (오류 0개)
- FsCheck 보드 불변 조건 5개 + Expecto 수렴 테스트 3개 이상 통과
- 특히 "TD 에이전트가 100k 자가 대국 후 랜덤 상대 승률 > 90%" 테스트 통과
- `grep "epsilon = 0" TicTacToe/src/TicTacToe/Training.fs` — winRateVsRandom에서 0.0 사용 확인
</verification>

<success_criteria>
1. Agent.fs: tdUpdate (V(s) += alpha * (V(s') - V(s))), randomAgent, tdAgent (ε-greedy, X 최대화/O 최소화)
2. Training.fs: playEpisode (self-play, prevBoard TD 백업), trainAgent (100k, history 반환), winRateVsRandom (epsilon=0)
3. ConvergenceTests.fs: testCase로 >90% 승률 테스트 포함
4. `dotnet test TicTacToe.sln` — 전체 통과, 실패 0개
5. 100k 에피소드 후 실제 >90% 승률 달성 (테스트로 검증됨)
</success_criteria>

<output>
완료 후 `.planning/phases/02-tictactoe-td-learning/02-02-SUMMARY.md` 생성
</output>
